<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.music_with_heart.dao.UserMsgMapper">
    <select id="findByUserMsg" parameterType="String" resultType="com.demo.music_with_heart.entity.UserMsg">
        select * from user_msg where user_id = #{user_id}
    </select>

</mapper>